# optiq-releases

Public release artifacts for the Optiq desktop app (signed, notarized macOS/Windows builds and the Tauri updater manifest). Managed entirely by CI in tryoptiq/optiq-health — see that repo's release workflows and docs/release-macos.md.
